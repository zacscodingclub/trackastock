<h2><%= @user.full_name %>'s Profile Page</h2>

<% if @user.stocks.count > 0 %>
  <%= render 'stocks/list' %>
<% else %>
  <p>
    This person isn't following any stocks.
  </p>
<% end %>

<%= link_to "Back", my_friends_path, class:"btn btn-primary" %>
